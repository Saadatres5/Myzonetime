# MyZoneTime — Environment Variables
# Copy this to .env and fill in values for local dev
# On Hostinger, set these via the Node.js app settings panel

# Node environment
NODE_ENV=production

# Port — Hostinger sets this automatically; keep default 3000 for local
PORT=3000
