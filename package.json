{
  "name": "myzonetime",
  "version": "1.0.0",
  "description": "MyZoneTime — World Clock & Time Zone Converter. Live time in 500+ cities.",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix apps/web && npm run build --prefix apps/web",
    "build:prod": "cross-env NODE_ENV=production npm run build",
    "dev": "npm run dev --prefix apps/web",
    "postinstall": "npm install --prefix apps/web && npm run build --prefix apps/web",
    "import:cities": "node scripts/import-world-cities.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tz-lookup": "^6.1.25"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2",
    "helmet": "^7.1.0"
  },
  "keywords": [
    "world-clock",
    "timezone",
    "time-converter",
    "meeting-planner",
    "hijri-calendar"
  ],
  "author": "MyZoneTime",
  "license": "MIT"
}
